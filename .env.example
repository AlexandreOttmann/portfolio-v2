NUXT_PRIVATE_RESEND_API_KEY=your_value

# AI chat (Petit-Oni) — see AI_CHAT_SETUP.md
ANTHROPIC_API_KEY=your_value
# Only for API keys not scoped to a workspace
ANTHROPIC_WORKSPACE_ID=
# Optional, defaults to claude-sonnet-5
AI_CHAT_MODEL=
# Optional: expose /api/chat-debug outside `nuxt dev` (never in production)
AI_CHAT_DEBUG=

# Chat logs (Supabase table ai_chat_interactions)
SUPABASE_URL=https://jobpwmdcwtsjsqwzfifb.supabase.co
SUPABASE_KEY=your_value
# Password of the /chat-logs page
BEST_PASSWORD=your_value
